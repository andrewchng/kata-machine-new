/**
 * Given an array of integers and a number k, find the total number of continuous sub-arrays whose sum equals to k. 
 */
export default function sub_array_sum(arr: number[], k: number): number {

}